[pytest]
asyncio_mode = auto
markers =
    asyncio: async tests
    integration: integration tests requiring databases
    e2e: end-to-end tests requiring all services
    slow: slow-running tests
    live: live integration tests requiring real LLM and infrastructure
